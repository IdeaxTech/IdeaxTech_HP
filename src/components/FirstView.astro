---
import firstViewImage from '../assets/unity_header_with_glass.png';
---

<section
	class="first-view"
	style={`background-image: url(${firstViewImage.src})`}
>
	<p class="first-view-text">
		いったん、<br />
		つくってみる
	</p>
</section>

<style>
	.first-view {
		position: relative;
		display: flex;
		align-items: center;
		justify-content: center;
		width: 100%;
		min-height: 814px;
		background-size: cover;
		background-position: center;
		margin-top: 7rem;
	}

	.first-view-text {
		font-family: 'hiragino-kaku-gothic-pron', sans-serif !important;
		position: absolute;
		left: 4rem;
		top: 33%;
		font-size: 4rem;
		color: #333333;
		text-align: left;
		font-weight: 600;
	}

	@media (max-width: 1024px) {
		.first-view {
			min-height: 600px;
			margin-top: 5rem;
		}

		.first-view-text {
			font-size: 3rem;
			left: 3rem;
		}
	}
	@media (max-width: 768px) {
		.first-view {
			min-height: 400px;
			margin-top: 4rem;
		}

		.first-view-text {
			font-size: 2rem;
			left: 2rem;
		}
	}
</style>
